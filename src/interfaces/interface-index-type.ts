// Interface que define un índice

interface ArrayCadenas {
  [index:number]: string;
}

let nombres:ArrayCadenas;
nombres = ['Víctor','Nacho','Marta'];

let n1:string = nombres[1];
console.log(nombre);
